# V5.8 Decision Anatomy

Kaynak: V5.7.1 B (n=32, Qwen 27B, think=false, n=280, `CMO_CONTRACT` stance satırı açık).  
`score_cmo_v51` değiştirilmedi. Canlı yeni benchmark yok. Production donduruldu.

## 1. Executive Summary

Qwen **çoğu vakada ticari duruş veriyor**. Resmi DECISION **0.875/2** bunu yansıtmıyor.

32 cevabın resmi dağılımı: DECISION=2 → 14, DECISION=1 → **0**, DECISION=0 → **18**. Skor ikili; 1 yok.

Elle ayrım (tam metin):

| Sınıf | n | % |
|---|---:|---:|
| REAL_OK | 12 | 37.5 |
| SCORER_MISS (duruş var, DEC=0) | 10 | 31.3 |
| REAL_DECISION_MISS | 8 | 25.0 |
| SCORER_FALSE_POS (DEC=2, duruş yok) | 2 | 6.3 |

Resmi DEC=0’ın **10/18’i (~56%) scorer kaçırması**, **8/18’i (~44%) gerçek erteleme/soru**.

Cevap: Qwen karar dilini bazı alanlarda (FOB, kabul etme, düşürmeyin, önceliğiniz) scorer’ın Almanya fiil listesinin dışında kuruyor. Asıl davranış açığı ince veride **güvenli varsayılanı olmayan** fiyat artışı ve “hangisini seç” soruları.

Evaluator’ı değiştirme. Bu rapor SCORER_MISS’i skor oyunu için kullanmaz.

## 2. Baseline

V5.7.1 B: CMO 10.219 · DECISION 0.875 · ACTION 1.75 · NATURAL 2.0 · first-shot 100% · fallback 0 · FC/UF/echo 0.

`_DECISION` fiilleri: `bırakmaz|düşürmez|öncelik|ben olsam|tercihim|öneririm|gitme|kal|ikinci test|hemen temas`.

DECISION=2 için bu fiil **ve** `_REASON` gerekir. `is_weak_decision` bu 32’de 0.

## 3. Decision Score Breakdown

| DEC | n | not |
|---|---:|---|
| 2 | 14 | 12 gerçek duruş + 2 false positive (`kalır` / `kalıp`) |
| 1 | 0 | ara kademe yok |
| 0 | 18 | 10 scorer miss + 8 real miss |

`kal` alt string: `kalite`, `kalır`, `kalıp`, `kalmak`, `kalırsınız`. Bir kısmı tesadüfen doğru DEC=2 üretir.

## 4. REAL_DECISION_MISS vs SCORER_MISS

**SCORER_MISS** — net duruş, fiil listesi tutmuyor:

- `incoterms`: «Bu aşamada alıcıya FOB sunup nakliye sorumluluğunu onlara bırakmak en güvenli yoldur.»
- `accept_order`: «Bu siparişi kabul etmeyin; aylık 20 ton kapasiteyle…»
- `lost_quote`: «Fiyatı hemen düşürmeyin» (`düşürmeyin` ≠ `düşürmez`)
- `germany_france`: «elden çıkarma» + «Fransa'yı ikinci bir test pazarı» (`ikinci bir test` ≠ `ikinci test`)
- `volume_vs_margin`: «hacmi riske atmanızı önermem» (`önermem` ≠ `öneririm`)
- `fair_leads` / `price_pressure` / `quote_conversion`: «önceliğiniz(i)» — Türkçe k→ğ, `öncelik` substring değil
- `moq`: varsayılan «standart koşulları korumak daha güvenli»
- `repeat_orders`: «ikinci siparişte fiyat avantajı sunun»

**REAL_DECISION_MISS** — tercih yok, soru veya «belirleriz»:

- `raise_price`, `sales_drop`, `new_buyer_priority`, `supplier_choice`, `distributor_pick`, `uncertain_data`, `new_country`, `mix_priority`

**SCORER_FALSE_POS:** `keep_customer` (`kalır`), `churn_risk` (`kalıp`) — resmi DEC=2, ticari olarak erteleme.

## 5. Decision Type Breakdown

Tüm 32 (dil sınıfı, resmi skordan bağımsız):

| Dil | n |
|---|---:|
| DIRECT_STANCE | 14 |
| CONDITIONAL_STANCE | 8 |
| DEFER | 5 |
| QUESTION_ONLY | 4 |
| HEDGE | 1 |
| MULTIPLE_OPTIONS | 0 |
| ACTION_WITHOUT_STANCE | 0 |

REAL_MISS içinde: QUESTION_ONLY 4 (`sales_drop`, `new_buyer_priority`, `uncertain_data`, `distributor_pick`) + DEFER 5 (`raise_price`, `supplier_choice`, `new_country`, `mix_priority`, `churn_risk`) + HEDGE 1 (`keep_customer`). `churn_risk` resmi DEC=2.

| Category | Decision score | Real misses | Scorer misses |
|---|---:|---:|---:|
| PRICE | 0.857 | 1 | 3 |
| CUSTOMER | 0.800 | 3 | 3 |
| MARKET | 0.667 | 3 | 1 |
| RISK | 1.333 | 0 | 1 |
| NEGOTIATION | 0.000 | 1 | 2 |
| CAPACITY | 2.000 | 0 | 0 |
| TIMING | 2.000 | 0 | 0 |

NEGOTIATION resmi 0.000: `incoterms` ve `moq` scorer miss; `supplier_choice` real miss. RISK yüksek çünkü «kabul etme» + `öncelikle`.

## 6. Language Pattern Analysis

Güvenli varsayılanı olan alanlarda Qwen fiil seçiyor: kabul etmem, FOB sun, düşürmeyin, önceliğiniz Nordöl.

Güvenli varsayılanı olmayan alanlarda kapanış kalıbı:

- «bu bilgileri paylaşırsanız … belirleriz / belirlerim / seçebiliriz»
- «net bir tercih yapmam mümkün değil»
- kullanıcının zaten verdiği ayrımı tekrar sorma (mevcut vs yeni)

`öncelikle` (zarf) scorer’a girer; `önceliğiniz` (isim, yumuşama) girmez. `Ben olsam` PRICE indiriminde çalışır, artışta kullanılmaz.

## 7. raise_price Analysis

Metin (DEC=0, total 9):

> Fiyatı artırmak için mevcut marj, kanal ve kapasite verileriniz elimde yok. … net bir artış kararı vermek riskli olur. … en doğru fiyat stratejisini **belirleriz**.

İstenen: «Şimdilik fiyatı artırmazdım; önce minimum marjı doğrulardım.»

**Kanıt: yalnızca prompt değil.** Aynı ailede `discount` (DEC=2): «Ben olsam … adım atmazdım.» V5.7.1 satırı «belirleriz ile bitirme» diyor; model `raise_price`’ta hâlâ bitiriyor.

Asimetri: fiyat **düşürme** varsayılanı hayır (zarar açık). Fiyat **artırma** varsayılanı model için «kanıt yoksa konuşmam» — geçici hayır değil. Bu pricing reasoning kalıbı: artış fırsat, indirim tehdit. Prompt satırı tehdit tarafını güçlendirdi, fırsat tarafını kapatmadı.

## 8. new_buyer_priority Analysis

Kullanıcı zaten ayrımı verdi: mevcut sipariş veriyor, yeni gelmiyor.

Zayıf (gerçek B metni): kanal kırılımı sor, «yönlendirebiliriz». Tercih yok, öncelik yok, trade-off yok, geçici duruş yok. Aksiyon: karşılaştırma isteği.

Güçlü olması gereken: mevcutı koru / kazanım deliği; reklam-fiyat dayatma yok; hacim doğrulanırsa öncelik değişir.

Fark: teşhis sorusu, kullanıcının verdiği olguyu **yeniden sormak** için kullanılıyor. `DIAGNOSTIC_CONTRACT` («en fazla 1–3 soru», hemen çözüm dayatma) stance satırıyla çatışıyor; teşhis kazandı.

## 9. collection_risk / incoterms Analysis

`collection_risk` (DEC=2): «kredi kaydı olmadan bu şartı **kabul etmem**. … ön ödeme veya banka garantisi.» Güvenli varsayılan: kredili büyük sipariş = hayır. Scorer `Öncelikle` ile 2; `kabul etmem` listede yok.

`incoterms` (DEC=0): «CIF … marjı riske bırakır. Bu aşamada **FOB sunmak** en güvenli yoldur.» Aynı güvenli varsayılan (açık riskli seçeneği alma). FOB/CIF fiil listesinde yok → SCORER_MISS.

Ortak aktarılabilir kalıp: **riskli seçeneğin güvenli varsayılanı hayır / daha az yükümlülük; kanıt gelince gevşe.** Pricing artışına: varsayılan artırma. Buyer priority: varsayılan mevcutı koru.

## 10. Strong Decision Examples

1. `market_choice` 13/2 — Almanya’yı ara, Fransa’ya geçici odaklanma; flip = somut FR teklif.
2. `discount` 12/2 — Ben olsam indirmem; marj+kanıt varsa sınırlı indirim.
3. `buyer_abc` 12/2 — Nordöl+Gastro A, Retail Mix düşük; hacim/marj flip.
4. `collection_risk` 10/2 — kabul etmem; L/C; geçmiş görünce değişir.
5. `capacity` 11/2 — büyük pazara girme; niş öneririm.
6. `cheap_price_market` 13/2 — 20 ton + düşük fiyat = girme; yalnızca net kâr alıcı.
7. `capacity_split` 11/2 — mevcut müşteri, yeni talebi ertele.
8. `incoterms` 9/0 — FOB (scorer 0, duruş 2).
9. `accept_order` 10/0 — kabul etmeyin (scorer 0).
10. `lost_quote` 9/0 — düşürmeyin (scorer 0).

## 11. Weak Decision Examples

1. `raise_price` 9/0 — belirleriz. DEFER.
2. `new_buyer_priority` 8/0 — verilen ayrımı tekrar sorar. QUESTION.
3. `sales_drop` 7/0 — kullanıcı «mevcut aynı» demiş; yine kaynak sorar. QUESTION.
4. `distributor_pick` 4/0 — ürün sor, seçebiliriz. QUESTION+DEFER.
5. `supplier_choice` 9/0 — tercih yapmam mümkün değil. DEFER.
6. `uncertain_data` 8/0 — kilitlemem + ürün sor. QUESTION.
7. `mix_priority` 10/0 — yönlendirme yapamıyorum. DEFER.
8. `new_country` 10/0 — lead yok ama «girme» demeden plan isteği. DEFER.
9. `keep_customer` 13/2 — «kesin karar temelsiz»; kalır false pos. HEDGE.
10. `churn_risk` 13/2 — kalıp + belirleriz. DEFER, false pos.

## 12. Root Cause

En fazla üç, kanıtlı:

1. **Scorer fiil listesi + Türkçe çekim.** `öncelik` ⊄ `önceliğiniz`; `düşürmez` ⊄ `düşürmeyin`; `öneririm` ⊄ `önermem`; `ikinci test` ⊄ `ikinci bir test`; `kal` ⊂ `kalite/kalır`. DEC=0’ın ~%56’sı bu. Evaluator değişmeyecek.

2. **Güvenli varsayılanı olmayan açık seçim.** Risk/FOB/indirim → hayır. Fiyat artışı / iki bilinmeyen tedarikçi / zorunlu DE-FR kilidi → «belirleriz». `discount` vs `raise_price` aynı prompt ailesinde zıt.

3. **Teşhis sözleşmesi stance’i eziyor.** `sales_drop` / `new_buyer_priority`: kullanıcı ayrımı verdi; model soru üretiyor. `DIAGNOSTIC_CONTRACT` + «1–3 soru» duruşun yerine geçiyor.

## 13. Commercial Decision Pattern

İyi CMO (Qwen’in kendi güçlü cevaplarından):

VERİ (ne var / ne yok, iddia ≠ olgu)  
→ TRADE-OFF (marj vs hacim, mevcut vs yeni, yükümlülük)  
→ GEÇİCİ DURUŞ (şimdilik A; riskli B’yi alma)  
→ FLIP KOŞULU (hangi kanıt A’yı bozar)  
→ AKSİYON (bu hafta kim, hangi teklif)

Zayıf kapanış: soru + «belirleriz». Aksiyon tek başına duruş sayılmaz; `repeat_orders` ve `fair_leads` duruş+aksiyon birlikte.

## 14. Recommended V5.8 Fix

Kod yok. Evaluator yok.

Tek hedefli aday (sonraki tur, isteğe bağlı):

| nereye | davranış | neden | risk |
|---|---|---|---|
| `CMO_CONTRACT` veya `DIAGNOSTIC_CONTRACT` **tek cümle** | Kullanıcının verdiği ayrımı yeniden sorma; teşhis sorusu duruşun yerine geçmez. Fiyat artışı varsayılanı: şimdilik artırma. | `raise_price` + `new_buyer_priority` REAL_MISS | False certainty; golden DE/FR’yi bozmamak için «kesin» yasak kalmalı |

**Yapılmayacak:** `_DECISION` genişletme (skor oyunu; 10 SCORER_MISS’i şişirir, 8 REAL_MISS’e dokunmaz).

## 15. What NOT To Change

Validator, evaluator, threshold, fallback, routing, model, canary %, think, n=280, voice, billing, API, V5.7.1 stance satırı (koru).

## 16. GO / NO-GO

**NO-GO evaluator.**  
**NO-GO** DECISION’ı fiil listesiyle kovalamak.  
**GO (sonraki dar tur):** yalnızca REAL_MISS üçlüsü — `raise_price` varsayılan hayır, `new_buyer_priority` verilen ayrımı kabul, teşhis≠duruş.  
**KEEP** V5.7.1 satırı ve %10 canary.

---

Soru: **Qwen gerçekten karar vermekte mi zorlanıyor, yoksa karar dili mi farklı?**

İkisi. Oran (n=32, V5.7.1 B):

- Karar dili scorer dışında: **10 SCORER_MISS** (~%31 tüm set, ~%56 resmi DEC=0).
- Gerçek duruş yok: **8 REAL_DECISION_MISS** (~%25). Artı 2 false-positive DEC=2.
- Gerçek güçlü duruş: **12 REAL_OK**.

En küçük davranışsal müdahale: evaluator değil; teşhis/fiyat-artışı varsayılanı için **en fazla bir cümle**, ve yalnız REAL_MISS kanıtına.
