# V5.4 Voice E2E Report

Voice kodu değiştirilmedi (`backend/tts_local.py`, `lib/speech.ts`, mic/speak, `/api/speak`).

## 1. Executive summary

Akış: mikrofon/STT (tarayıcı Web Speech) → commercial intent → Qwen 27B think=false → validator → XTTS.

STT backend’de yok; Test C’de STT çıktısı olarak golden soru kullanıldı (transkript sözleşmesi).

**Voice E2E: PASS**

## 2. V5.3 baseline

XTTS+Qwen peak ≈ 19925 MiB / 23040 MiB. Headroom ≈ 3 GB.

## 3–6. Shadow / scenarios / golden

Kalite: `V5.4_SHADOW_CANARY_REPORT.md`. Golden thinking TTS’ye gitmedi.

## 7–8. Adversarial / validator

Thinking leakage unit test PASS. Validator REJECT gevşetilmedi.

## 9. done_reason

Qwen length `0.364`.

## 10. Latency (voice)

İlk TTS çağrısı XTTS warmup içerir (~52 s). Sonraki sentezler gerçek konuşma süresine daha yakındır.

| stage | s |
|---|---|
| STT (browser / simulated) | 0.0 |
| LLM (Qwen, Test C) | 7.109 |
| TTS warmup + ilk sentez (Test B) | 51.971 |
| TTS sentez (Test C, XTTS resident) | 14.458 |
| TTS uzun metin (Test D) | 20.182 |
| user-perceived LLM+TTS (resident, Test C) | 21.567 |

Thinking TTS’ye gitmedi. Engine = **xtts**. WAV Test B/C/D tamam.

## 11. VRAM

| test | peak MiB | note |
|---|---|---|
| idle (Qwen resident, XTTS off) | 17589.0 | Llama3 unload sonrası |
| A Qwen only | 17589.0 | dedicated VRAM |
| B XTTS + Qwen | 20111.0 | headroom ≈ 2929 MiB |
| C STT-sim → Qwen → XTTS | 20111.0 | CUDA OOM yok |
| D long context → Qwen → XTTS | 20111.0 | |
| Llama3 + Qwen (keep_alive çakışması) | 22617.0 | XTTS’e yer kalmaz |
| CUDA OOM | False | |
| model reload | load_s=0.005 | Qwen resident |

## 12. Voice E2E observations

- thinking leakage: `False`
- incomplete wav: `False`
- interruption: `False`
- engine: `xtts`
- error: ``

## 13–14. Canary / rollback

Canary voice trafiğini rastgele bölmez (session-hash). Rollback Llama3.

## 15. Tests

`test_thinking_never_tts`, `test_qwen_voice_contract`, `test_shadow_does_not_reach_tts`.

## 16. Remaining risks

- Gerçek mikrofon/STT bu turda tarayıcı Web Speech; backend STT yok (simüle transkript)
- XTTS ilk yükleme uzun; resident sentez ~14–20 s
- XTTS + 27B peak 20111 / 23040 MiB (~2.9 GB headroom)
- Production Llama3 keep_alive + shadow Qwen 30m birlikte 22617 MiB bırakır; bu yüzden shadow `keep_alive=0`
- Kullanıcı algılanan gecikme: Qwen ~15 s + XTTS ~14 s

## 17. Production recommendation

**PRODUCTION CANDIDATE**. Voice identity değişmedi. Production default llama3. Qwen+XTTS bu GPU’da OOM olmadan çalıştı; first-shot validator %90 olduğu için READY değil.
