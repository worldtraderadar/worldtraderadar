# V5.2 Qwen3.5 27B Gerçek Donanım Benchmarkı

Tarih: 2026-09-02  
Production `OLLAMA_REASONING_MODEL` = **llama3** (değiştirilmedi).  
35B kurulmadı, test edilmedi, karar verilmedi.

## Model kurulumu

`ollama pull qwen3.5:27b` **başarılı**.

| Alan | Değer |
|---|---|
| Tag | `qwen3.5:27b` |
| ID | `7653528ba5cb` |
| Mimari | qwen35 |
| Parametre | 27.8B |
| Quantization | **Q4_K_M** |
| Dosya boyutu | **17.42 GB** |
| Context (model kartı) | 262144 |
| Yetenek | completion, vision, tools, **thinking** |
| Durum | MODEL KURULU / TEST EDİLDİ |

## Donanım

NVIDIA Quadro RTX 6000 — dedicated **23040 MiB (~22.5 GB)**. Shared GPU Memory VRAM değildir. System RAM **254.7 GB**.

### TEST A — XTTS kapalı / MODEL_ONLY_VRAM

Uvicorn/XTTS durduruldu. Idle dedicated **1007 MiB**.

| Ölçüm | Değer |
|---|---|
| Idle dedicated | 1007 MiB |
| Load sonrası dedicated | 17814 MiB |
| Load wall | **31.0 s** (ilk yükleme) |
| **MODEL_ONLY_VRAM (delta)** | **16807 MiB (~16.4 GB)** |
| Inference peak dedicated | **17948 MiB** |
| Shared GPU Memory | ~890 MB (849–890 MiB bandı) |
| System RAM used | ~19–26 GB (254.7 GB total; model RAM’e yayılmadı) |
| GPU util peak | 92% |
| Avg latency / call | **17.13 s** |
| tokens/sec | **21.65** |
| eval_count | her çağrıda **280** (tavan) |
| Crash / timeout | yok |
| **OFFLOAD** | **NO** — +16.4 GB dedicated, ~22 tok/s, GPU-resident |

Dosya 17.42 GB; runtime dedicated ~16.4–17.5 GB + KV/overhead. 22.5 GB karta **yalnızca model** olarak sığar.

### TEST B — XTTS açık / üretim koşulu

XTTS yeniden başlatıldı (`engine=xtts`, `device=cuda`, ready).

Önemli: Test A’dan sonra Qwen GPU’da kaldı. Test B baseline **19881 MiB** temiz “yalnız XTTS” değil; **XTTS + hâlâ oturan 27B**. V5.1’deki ~12.7 GB XTTS-only ile karıştırılmamalı.

| Ölçüm | Değer |
|---|---|
| Baseline (XTTS + resident 27B) | 19881 MiB |
| Load probe wall | 1.25 s (model zaten yüklü) |
| Inference peak dedicated | **19925 MiB** |
| Free dedicated | **~2925 MiB** |
| Shared GPU Memory | ~969 MB |
| System RAM used | ~29 GB |
| GPU util peak | 91% |
| Avg latency | **17.05 s** |
| tokens/sec | **21.71** (Test A ile aynı) |
| Crash / timeout | yok |
| **OFFLOAD** | **NO evidence of CPU decode** — tok/s düşmedi. Kart dolu (~19.4 GB / 22.5 GB). XTTS, 27B yanında V5.1’deki 12.7 GB’ı alamadı; sıkışık birlikte oturma. |

256 GB RAM “GPU inference başarısı” sayılmaz. Burada decode hızı GPU’da kaldı; asıl risk **VRAM headroom ~3 GB**.

## Llama3 vs Qwen3.5 27B vs fallback

Aynı 20 V5.1 senaryosu. Llama3/fallback sayıları V5.1’den (aynı input). Qwen bu turda koşuldu.

| MODEL | CMO SCORE | REAL LLM SUCCESS | FALLBACK RATE | BRIEF ECHO | ENGLISH | UNSUPPORTED FACT | FALSE CERTAINTY | MEMORY DISHONESTY | AVG LATENCY | TOKENS/SEC | MODEL-ONLY PEAK VRAM | XTTS+MODEL PEAK VRAM | PEAK SYSTEM RAM | OFFLOAD |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Llama3 | 8.3** | **10%** | **90%** | **60%** | 0% | 0% | 10% | — | 5.38 s | 76.3 | — | 12744 MiB (V5.1, XTTS+llama) | — | no evidence |
| **Qwen3.5 27B Q4_K_M** | 8.3** | **0%** | **100%** | 0%* | 0% | 0% | 15%* | **0%** | **17.1 s** | **21.7** | **17948 MiB** | **19925 MiB** | ~29 GB used / 254.7 | **NO** |
| Commercial fallback | 5.4† | 0% | 100% | 0% | 0.5† | 0% | 0% | — | ms | — | — | — | — | n/a |

\* Brief echo 0%: model **boş `message.content`** döndü; echo ölçülemedi. Final fallback. False certainty 15% final/fallback metin heuristic’i.  
\*\* Final CMO çoğu fallback. LLM başarısı değil.  
† V5.1: şablonsuz boş cevaplar ortalamayı bozdu.

**REAL_LLM_SUCCESS Qwen = 0/20.** Llama3’ten **daha düşük**. Fallback oranı düşmedi; **%90 → %100**.

## Neden Qwen LLM cevabı boş?

`qwen3.5:27b` **thinking** modeli. Ollama `message.content` + `message.thinking`.

Benchmark/production compose yalnızca `content` okur (`num_predict=280` benchmark’te). 20/20 çağrıda `eval_count=280` ve `content=""`. Thinking bütçeyi bitirdi; validator boş cevabı REJECT etti; retry’e girmedi (`cleaned` boş); commercial/playbook fallback.

Ayrı diagnostik (`num_predict=800`): yine `CONTENT_LEN=0`, `THINKING_LEN=2746`, 800 token’ın hepsi thinking. Thinking **İngilizce** ve “iç brief kopyala”yı yanlış okuyan meta-plan. Kullanıcıya giden ticari Türkçe **üretilmedi**.

Bu, production kodunu gevşetmeden ölçülen gerçek entegrasyon sonucu. Thinking’i `content` saymak LLM başarısı uydurmak olurdu; yapılmadı.

## Almanya vs Fransa

Soru: *Almanya'dan 3 müşteri buldum ama fiyat baskısı çok yüksek. Fransa'ya mı yönelmeliyim?*

| | Qwen 27B (A ve B aynı) |
|---|---|
| Path | **COMMERCIAL_FALLBACK** |
| Validator direct | REJECT (boş) |
| Retry | yok |
| Brief echo | ölçülemedi (boş raw) |
| Hatırlıyorum | yok (boş raw) |
| Markdown leak | yok (boş raw) |
| False certainty (final) | False |
| CMO final /14 | **13** (fallback şablonu) |
| Model düşündü mü? | GPU’da thinking token üretti; **kullanıcıya ticari karar yazmadı** |

**LLM direct raw:** `(boş)`  
**LLM retry raw:** `(yok)`  
**Final:** V5.1 commercial fallback metni (Almanya’yı bırakma, fiyat baskısı iddia, Fransa ikinci test, 1 soru).

Qwen bu vakada fallback sayesinde iyi görünüyor. **LLM reasoning başarısı değil.**

## 20 senaryo (Test A)

Hepsi `direct=REJECT`, `retry` yok, `real_llm=False`. Path commercial veya playbook fallback.

## Production uygunluğu

1. Ticari reasoning (kullanıcıya giden): **yok** — 0 PASS  
2. REAL_LLM_SUCCESS: **0%** (Llama3 %10)  
3. Fallback: **%100** (Llama3 %90) — düşmedi  
4. Brief echo: raw boş; Llama3 %60 echo problemi Qwen’de ölçülememedi  
5. Türkçe doğallık: kullanıcı metni fallback  
6. Hallucination: content boş  
7. False certainty: model raw yok  
8. Latency: **17 s** vs Llama3 **5.4 s** (~3× yavaş)  
9. VRAM: model-only **~16.4 GB** sığar; XTTS+model **~19.4 GB / 22.5 GB**, ~3 GB boş  
10. XTTS birlikte: crash yok, tok/s aynı; headroom dar

## Sorular

**Qwen3.5 27B, Llama3’e göre ticari reasoning açısından gerçekten fark yarattı mı?**  
Hayır. Mevcut CMO pipeline’da **daha kötü**: 0% vs 10% REAL_LLM_SUCCESS. Fark, 27B’nin thinking çıktısını `content`’e dökmemesi.

**XTTS ile aynı makinede kullanılabilir mi?**  
Teknik olarak **çalıştı** (crash yok, ~22 tok/s). Dedicated peak ~19.9 GB. Headroom ~3 GB. Temiz 12.7 GB XTTS + 16.4 GB 27B = 29 GB **sığmaz**; pratikte birlikte oturma sıkışık / kısmi yerleşim. Rahat production değil.

**24 GB Quadro’da production için mantıklı mı?**  
**Şu an hayır.** Model kartına yalnız sığar, CMO cevabı üretmez, 3× yavaş, XTTS ile VRAM dar.

**Fallback oranı ne kadar düştü?**  
Düşmedi. **%90 → %100**.

**Gerçek LLM başarı oranı ne?**  
**%0** (20/20 fallback).

**Almanya-Fransa’da model gerçekten düşünüyor mu?**  
GPU thinking üretti; kullanıcıya karar **yazmadı**. Final fallback.

## RECOMMENDATION

**C) NO CLEAR WINNER** (Qwen 27B şu pipeline’da production adayı değil; Llama3’ü de geçmedi.)

Daha sıkı okuma: thinking entegrasyonu yapılmadan “27B daha iyi CMO” iddiası **kanıtlanmadı**. Env değiştirilmedi.

35B: karar yok, kurulmadı.

## Testler

- Eski **106/106** korundu  
- V5.2 unit: `test_brain_v52.py` (aynı 20 case, production model qwen’e çekilmedi)  
- Ses / billing / public API **değiştirilmedi**  
- XTTS koduna dokunulmadı; süreç Test A’da durdurulup Test B’de yeniden açıldı (şu an `/health` tts=xtts cuda)

## Dosyalar

Eklenen: `backend/benchmark_v52.py`, `backend/test_brain_v52.py`, `backend/benchmark_v52_results.json`, bu rapor.  
Değişmeyen: `tts_local.py`, mic/speak, billing, `OLLAMA_REASONING_MODEL`.
