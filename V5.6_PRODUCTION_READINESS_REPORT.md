# V5.6 Production Readiness Report

Production `OLLAMA_REASONING_MODEL` = **llama3** (otomatik değiştirilmedi).  
Canary routing: SHA-256 session bucket, değişmedi. think=false, n=280. Shadow=OFF.

## 1. Executive Summary

Soru artık "Qwen daha iyi mi?" değil: **"Qwen production default olacak kadar operasyonel olarak sağlam mı?"**

**RECOMMENDATION: KEEP_LLAMA_DEFAULT**  
**GATE: PRODUCTION CANDIDATE**

- CMO 9.8 < 10

Qwen first-shot V5.5 `0.9` → V5.6 `0.95`.  
VRAM dual-resident V5.5 ~163 MiB free. Mode B Qwen+XTTS headroom `3031.0` MiB.  
Unit tests: 190/190 PASS. `OLLAMA_REASONING_MODEL` değiştirilmedi.

## 2. V5.5 Baseline

| metrik | V5.5 Qwen | V5.5 Llama |
|---|---|---|
| REAL_LLM | 0.95 | 0.25 |
| first-shot | 0.9 | 0.15 |
| CMO | 10.3 | 6.95 |
| length | 0.318 | 0.568 |
| p95 | 18.026 | 6.349 |
| VRAM | dual-resident ~22602, free ~163 MiB | aynı stack |

Tek Qwen fallback: `collection_risk`, first+retry REJECT, done_reason=length.

## 3. Validator Analysis

Hedef first-shot >=95%. Validator gevşetilmedi (kaynaksız rakam / false certainty / memory hâlâ REJECT).

V5.5 Qwen first-shot %90 = 18/20 PASS. İki REJECT:

1. `raise_price` — first REJECT, retry PASS (kalite/format; retry kurtardı).
2. `collection_risk` — first+retry REJECT, done=length (kesilme + validator).

V5.6 Qwen REJECT satırları:

- `collection_risk` first=REJECT retry=PASS done=stop cause=E unsupported claim reasons=['kaynaksız rakam: %30']

Sınıflar: A gerçek kalite, B false positive (kullanılmadı / validator gevşetilmedi), C truncation, D format, E unsupported claim, F başka.

**First-shot V5.6: 0.95** (Evet, >=95%).

## 4. Done Reason Analysis

num_predict **280 korundu** (kör yükseltme yok).

| | V5.6 Qwen |
|---|---|
| done_reason=length | 0.0 |
| length ∩ first REJECT | 0.0 |
| length + complete + first PASS | 0.0 |

Length token tavanıdır. V5.6'da Qwen `done_reason=length` **0**. Kesilme kaynaklı first-shot reject kalmadı. `collection_risk` reject'i truncation değil; kaynaksız `%30` (sınıf E). num_predict 280 kaldı.

## 5. VRAM / Lifecycle Analysis

Ollama keep_alive=30m varsayılan. SAFE mod `keep_alive=0` ile eş reasoning modelini unload eder. vLLM/llama.cpp yok.

### A) CURRENT RESIDENT MODE

- after llama: used=20339.0 free=2398.0 MiB
- after both: used=22457.0 free=280.0 MiB
- after TTS: used=22457.0 free=280.0 MiB
- llama load_s: 0.004
- qwen load_s: 0.005
- peak: 22457.0
- TTS ok: None latency None

### B) SAFE MODEL LIFECYCLE MODE

- after unload llama: used=17492.0 free=5246.0 MiB
- qwen-only: used=17492.0 free=5246.0 MiB
- qwen load after llama unload: 0.004 wall 2.393s
- after peer unload: used=17492.0 free=5246.0 MiB
- Qwen+XTTS: used=19706.0 free=3031.0 MiB
- llama reload wall: 6.163s load_s 3.712
- TTS ok: True latency 32.044

CUDA OOM: `False`.  
Safe XTTS headroom (>= 1024 MiB): `True` (B free=3031.0).

MODEL_CANARY_UNLOAD_PEER default **false** (V5.5 latency korunur). SAFE mode ölçülmeden production default açılmaz.

## 6. Latency Analysis

Qwen avg/p50/p95: 11.848 / 11.677 / 14.851  
prompt_eval / gen / load: 3.671 / 6.946 / 0.005  
tok/s: 19.9

p95 gizlenmedi. ~18s sınıfı kullanıcıya uzun gelir; voice ile toplam daha da büyür.

## 7. Voice E2E

TTS engine `xtts` ok `True` latency `18.107` thinking `False`.

REAL_MIC_STT: `NOT AVAILABLE`  
time_to_first_text: `NOT AVAILABLE`  
time_to_final_text: `NOT AVAILABLE`  
time_to_audio_start: `18.107`  
time_to_audio_end: `NOT AVAILABLE`  
total_user_latency: `NOT AVAILABLE`

Sentetik transcript gerçek mikrofon yerine geçmez.

## 8. Golden Case

Soru: `Almanya'dan 3 müşteri buldum ama fiyat baskısı çok yüksek. Fransa'ya mı yönelmeliyim?`  
path `LLM_CMO_SUCCESS` first `PASS` retry `` CMO `13`  
checks `{'keep_germany': True, 'price_claim': False, 'france_second': True, 'no_false_fr': True, 'real_llm': True, 'no_markdown_fallback': True}`

```
Almanya'daki üç potansiyel müşteriyi hemen elden çıkarma; henüz net bir fiyat teklifi ve marj hesabı yok. Fransa'ya yönelmek, mevcut fırsatları test etmeden ikinci bir pazar açmak anlamına gelir. Önceliğin yüksek hacim mi yoksa daha iyi kar marjı mı olduğunu belirle. Eğer Almanya'daki üçü de kabul edilemez fiyat istiyorsa, o zaman Fransa'yı ikinci test pazarı olarak devreye alırım. Şu an yapman gereken, bu üç firmaya net teklif verip geri dönüşleri karşılaştırmak.
```

## 9. Adversarial / Security

BRIEF_ECHO `0.0` ENGLISH `0.0` FALSE_CERTAINTY `0.0` UNSUPPORTED_FACT `0.0`  
Hallucination/false certainty/unsupported fact: 0 hedefi. Thinking kullanıcıya gitmez.

## 10. 10% Canary

Split 1000: Qwen `10.0%` Llama `90.0%`  
Routing V5.4/V5.5 ile aynı (`v55-live-0` bucket 25, `v55-live-10` bucket 0).

## 11. 25% Canary

Split: Qwen `26.0%` / Llama `74.0%` (n=1000, qwen=260). Beklenen ~%75/%25.  
Live: sticky=True bind=qwen3.5:27b germany `LLM_CMO_SUCCESS` first=PASS CMO=14 collection `LLM_CMO_SUCCESS` first=PASS retry=—

## 12. 50% Canary

Split: Qwen `49.8%` / Llama `50.2%` (n=1000). Beklenen ~%50/%50.  
Live: sticky=True bind=qwen3.5:27b germany `LLM_CMO_SUCCESS` first=PASS CMO=14 collection `LLM_CMO_SUCCESS` first=PASS retry=—

## 13. 100% Explicit Qwen Path

`MODEL_CANARY_PERCENT=100`, `OLLAMA_REASONING_MODEL` **hâlâ llama3**.  
Split: Qwen `100.0%`.  
Full 20-case bu path'te. Live: sticky=True bind=qwen3.5:27b germany `LLM_CMO_SUCCESS` first=PASS CMO=14 collection `LLM_CMO_SUCCESS` first=PASS retry=—

## 14. Rollback

- 25 → 0 ok=True assigns_qwen=False
- 50 → 0 ok=True assigns_qwen=False
- 100 → 0 ok=True assigns_qwen=False

## 15. Production Gates

| gate | hedef | V5.6 Qwen |
|---|---|---|
| REAL_LLM | >=95% | 1.0 |
| first-shot | >=95% | 0.95 |
| fallback | <=5% | 0.0 |
| BRIEF_ECHO | 0 | 0.0 |
| hallucination / FC / UF | 0 | 0.0 / 0.0 |
| golden | PASS CMO>=13 | LLM_CMO_SUCCESS CMO 13 |
| CUDA OOM | 0 | False |
| empty | 0 | 0.0 |
| CMO | >=10 | 9.8 |
| XTTS headroom | >= 1024 MiB | 3031.0 |
| tests | PASS | True |

## 16. Remaining Risks

- Dual-resident (Mode A) free ~280 MiB; Qwen 20-case peak 22608 used / ~129 MiB free. Production default için yetersiz.
- SAFE mode (llama unload) Qwen+XTTS free **3031 MiB**. Switch maliyeti: llama reload wall 6.163s / load_s 3.712s.
- CMO 9.8 < 10 (V5.5 10.3). Kısa tamamlanmış cevap length'i 0 yaptı ama CMO skorunu 0.5 düşürdü.
- `collection_risk` first-shot hâlâ kaynaksız `%30` ile REJECT (sınıf E, validator doğru). Retry PASS.
- Golden 20-case `price_claim` keyword kaçırdı; metin marj hesabı yok diyor, CMO 13 / REAL_LLM.
- Qwen p95 14.851s (V5.5 18.026s); Llama ~6s. Kullanıcı latency Llama kadar değil.
- REAL_MIC_STT yok; `/consult/stream` bu koşuda ConnectError.
- %100 canary ≠ production default.

## 17. Final Recommendation

**KEEP_LLAMA_DEFAULT**

First-shot %95 ve REAL_LLM %100 yeterli değil: CMO 9.8 < 10. Qwen canary %10'da kalmalı; OLLAMA_REASONING_MODEL elle qwen yapılmamalı.
